spring:
  application:
    name: ecommercemg
  datasource:
    url: jdbc:mysql://localhost:3306/ecommercemg?createDatabaseIfNotExist=true
    username: root
    password: magaby2280@
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}